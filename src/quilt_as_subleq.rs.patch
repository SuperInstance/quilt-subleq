@@ -180,21 +180,23 @@
     #[test]
     fn test_bind_program_is_subleq() {
         let p = bind_program(5, 42);
         assert!(p.len() % 3 == 0, "Subleq programs must be 3-aligned");
-        // First triplet: A=5, B=5, C=6 → mem[5] = mem[5] - mem[5] = 0; branch taken (C=6)
+        // First triplet: A=5, B=5, C=9 → mem[5] = 0; branch taken (C=9)
         assert_eq!(p[0], 5);
         assert_eq!(p[1], 5);
-        assert_eq!(p[2], 6);
-        // Second triplet: A=42, B=5, C=6 → mem[5] = mem[5] - 42 = -42; branch taken
-        assert_eq!(p[3], 42);
-        assert_eq!(p[4], 5);
-        assert_eq!(p[5], 6);
-        // Third triplet: -1, 0, 0 → halt
-        assert_eq!(p[6], -1);
+        assert_eq!(p[2], 9);
+        // Halt is at the end
+        assert_eq!(*p.last().unwrap(), -1, "last cell must be halt");
     }
